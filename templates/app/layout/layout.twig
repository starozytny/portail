{% extends 'base.twig' %}

{% block stylesheets %}
    {% webpack_entry_css 'app' %}
{% endblock %}

{% block body %}
    <div class="body">
        <nav class="fixed">
            <div class="nav-header">
                <div class="nav-header-logo">
                    <span class=nav-header-logo-span>{{ session.user[2] }}</span>
                </div>
            </div>
            <div class="nav-mobile">
                <span class="icon-menu"></span>
            </div>

            {% set menu = [
                {
                    'path': pathHomepage,
                    'icon': 'home',
                    'label': 'Accueil',
                    'name': 'espace-client'
                },
                {
                    'path': pathEdls,
                    'icon': 'paste',
                    'label': 'Etats des lieux',
                    'name': 'etats-des-lieux'
                },
                {
                    'path': pathHomepage,
                    'icon': 'plus',
                    'label': 'Ajouter état des lieux',
                    'name': 'ajouter-edl'
                },{
                    'path': pathHomepage,
                    'icon': 'account',
                    'label': 'Mon compte',
                    'name': 'mon-compte'
                },
                {
                    'path': pathLogout,
                    'icon': 'logout',
                    'label': 'Se déconnecter',
                    'name': 'logout'
                },
            ] %}
            <div class="nav-body">
                <div class="items">
                    {% for item in menu %}
                        <div class="item">
                            <a href="{{ item.path }}" data-name="{{ item.name }}">
                                <span class="icon-{{ item.icon }}"></span>
                                <span>{{ item.label }}</span>
                            </a>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </nav>

        <main>
            <header>
                <div class="header-container">
                    <div class="header-name">
                        <div class="fokus">
                            <span class="txt-color1">Fokus</span>
                            <span>Logiciel d'état des lieux</span>
                        </div>
                        <div class="profil">
                            <span>265 crédits restants</span>
                            <div>
                                <a class="btn-icon" href="{{ pathLogout }}">
                                    <span class="icon-help"></span>
                                    <span class="tooltip">Besoin d'aide ?</span>
                                </a>
                                <a class="btn-icon" href="{{ pathLogout }}">
                                    <span class="icon-logout"></span>
                                    <span class="tooltip">Se déconnecter</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </header>

            {% block main_content %}{% endblock %}
        </main>
    </div>
{% endblock %}

{% block javascripts %}
    {% webpack_entry_js 'app' %}
{% endblock %}